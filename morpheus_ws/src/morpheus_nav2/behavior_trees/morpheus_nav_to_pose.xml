<!--
  Morpheus rover custom behavior tree for NavigateToPose.

  Additions over the stock Nav2 tree:
    1. ArUco marker detection — when a marker is spotted mid-navigation,
       the rover pauses for 3 seconds (simulating a photograph) then resumes.
    2. Fast recovery — clear costmaps + spin only (backup removed: terrain always blocks it).
       3 retries max so the rover skips unreachable waypoints quickly.
-->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="3" name="NavigateRecovery">
      <PipelineSequence name="NavigateWithReplanning">

        <!-- Replan global path at 5 Hz -->
        <RateController hz="5.0">
          <RecoveryNode number_of_retries="1" name="ComputePathToPose">
            <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
            <ClearEntireCostmap name="ClearGlobalCostmap-Context"
              service_name="global_costmap/clear_entirely_global_costmap"/>
          </RecoveryNode>
        </RateController>

        <!-- Follow the path, but check for ArUco markers reactively -->
        <ReactiveFallback name="FollowOrPhoto">
          <!-- Branch 1: ArUco detected → pause 3 s (photograph), then resume -->
          <Sequence name="ArucoPhotoSequence">
            <IsArucoDetected topic="/aruco_markers" timeout="1.0"/>
            <Wait wait_duration="3"/>
          </Sequence>
          <!-- Branch 2: normal path following with local costmap recovery -->
          <RecoveryNode number_of_retries="1" name="FollowPath">
            <FollowPath path="{path}" controller_id="FollowPath"/>
            <ClearEntireCostmap name="ClearLocalCostmap-Context"
              service_name="local_costmap/clear_entirely_local_costmap"/>
          </RecoveryNode>
        </ReactiveFallback>

      </PipelineSequence>

      <!-- Recovery: clear costmaps and wait for sensor refresh, no spin.
           Spin is omitted because EKF yaw noise on rough terrain causes the
           Spin behavior to never detect completion, blocking for minutes. -->
      <ReactiveFallback name="RecoveryFallback">
        <GoalUpdated/>
        <RoundRobin name="Recovery">
          <Sequence name="ClearAndWait">
            <ClearEntireCostmap name="ClearLocal"
              service_name="local_costmap/clear_entirely_local_costmap"/>
            <ClearEntireCostmap name="ClearGlobal"
              service_name="global_costmap/clear_entirely_global_costmap"/>
            <Wait wait_duration="1"/>
          </Sequence>
          <Sequence name="ClearAndWait2">
            <ClearEntireCostmap name="ClearLocal2"
              service_name="local_costmap/clear_entirely_local_costmap"/>
            <Wait wait_duration="2"/>
          </Sequence>
        </RoundRobin>
      </ReactiveFallback>
    </RecoveryNode>
  </BehaviorTree>
</root>
